apiVersion: v1
kind: Service
metadata:
  name: todo-backend-nodeport
  namespace: todo-app
spec:
  selector:
    app: todo-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080  # Specify node port for external access
  type: NodePort