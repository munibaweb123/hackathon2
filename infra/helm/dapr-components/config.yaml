apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: dapr-config
spec:
  metric:
    enabled: true
  tracing:
    samplingRate: "1"
    zipkin:
      endpointAddress: http://jaeger-collector.monitoring:9411/api/v2/spans
  logging:
    apiLogging:
      enabled: true
